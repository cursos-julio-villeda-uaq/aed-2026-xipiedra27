# Explicación — Actividad 9

Completa este archivo con tus propias palabras.

1. Diferencia principal entre la solución iterativa y la recursiva:
La iterativa suma usando el ciclo for y la recursiva hace la suma llamandose a ella misma

2. Caso base de `sumaRecursiva` y por qué es necesario:
El caso base es cuando n es igual a 0 y regresa 0

3. Condición que hace terminar a `sumaIterativa`:
El ciclo termina cuando i llega a 0 

4. SHA del commit final entregado en Classroom:
0dfa0b3
