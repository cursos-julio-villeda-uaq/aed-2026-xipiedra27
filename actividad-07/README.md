# Actividad 7.1 — Promedio con vector

Implementa las funciones declaradas en `include/promedio.hpp` dentro de `src/promedio.cpp`.

Puedes modificar:

- `src/promedio.cpp`
- `src/main.cpp`
- `EXPLICACION.md`

No modifiques `tests/`, `CMakeLists.txt` ni el flujo de GitHub Actions.

La función `calcularPromedio` debe lanzar `std::invalid_argument` cuando reciba un vector vacío.
